import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.kcarwash.com',
  appName: 'K CARWASH',
  webDir: 'build',
  server: {
    androidScheme: 'https'
  }
};

export default config;
